*** Begin Patch
*** Update File: prisma/schema.prisma
@@
 model User {
   id              String    @id @default(cuid())
   email           String    @unique
   name            String
   image           String?
   provider        String
   password        String?
   phone           String?
   phoneVerifiedAt DateTime?
-  role            String    @default("cliente")
+  role            Role      @default(cliente)
   createdAt       DateTime  @default(now())
   updatedAt       DateTime  @updatedAt
   cliente         Cliente?
 }
@@
 model Cliente {
@@
-  estado           String   @default("pendiente") // pendiente | aprobado | rechazado
+  estado           ClienteEstado   @default(pendiente)
@@
 }
@@
 model Contrato {
@@
-  estado       String
+  estado       ContratoEstado
@@
 }
@@
 model Pago {
@@
-  estado        String
+  estado        PagoEstado
@@
 }
@@
 model Factura {
@@
-  afipEstado   String    @default("pendiente")
+  afipEstado   AfipEstado    @default(pendiente)
@@
 }
+
+// Enums
+enum Role {
+  admin
+  operador
+  cliente
+}
+
+enum ClienteEstado {
+  pendiente
+  aprobado
+  rechazado
+}
+
+enum ContratoEstado {
+  activo
+  finalizado
+  cancelado
+  pendiente
+}
+
+enum PagoEstado {
+  pendiente
+  pagado
+  vencido
+}
+
+enum AfipEstado {
+  pendiente
+  emitida
+  rechazado
+}
*** End Patch
